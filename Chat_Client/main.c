#include "main.h"

int main() 
{

   // pthread_t thread1, thread2;

   // // sending stuff over thread
   // pthread_create(&thread1, NULL, thread1, NULL);

   // // recieving stuff over thread
   // pthread_create(&thread2, NULL, thread2, NULL);

   sender_handler();

   

}

